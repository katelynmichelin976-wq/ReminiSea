# 忆海拾光 v5.0 · 腾讯云迁移设计方案

> 设计日期：2026-05-05
> 状态：计划（待实施）
> 前提：v4.x 功能稳定、妈妈试用反馈闭环后启动

---

## 一、背景与动机

### 为什么迁移

| 现状（v4.x） | 问题 |
|-------------|------|
| Supabase (supabase.co) | 被墙，国内用户需 VPN 访问 |
| 邮箱+密码登录 | 不贴合国内手机验证码习惯 |
| 服务器在海外 | 数据合规风险（国内用户数据需存储在国内） |
| 仅 PWA | 无社交传播入口，拉新成本高 |

### v5.0 目标

1. **数据合规** — 所有数据存储在国内服务器
2. **手机验证码登录** — 降低使用门槛
3. **小程序入口** — 微信生态拉新
4. **App 可延续** — 数据平滑迁移到独立 App
5. **PWA 继续可用** — 现有用户无缝过渡

### 技术选型

选 **腾讯云 CloudBase** 而非自建后端（SCF + PostgreSQL + COS）的原因：

| | CloudBase | 自建 SCF + PostgreSQL |
|---|---|---|
| 小程序集成 | **原生 SDK 直连数据库** | 所有操作需写云函数中转 |
| 数据直连 | 小程序端 `db.collection(...).get()` | 必须走 REST API |
| 开发工作量 | 少（小程序端~0 后端代码） | 多（每数据操作写一个云函数） |
| Schema | NoSQL 文档型（需改造） | PostgreSQL 不变 |
| 数据迁移 | 需 ETL（6 表→3 集合） | 可直接导入 |

**选择 CloudBase 的理由：** 小程序接入成本差异巨大。CloudBase 让小程序端直连数据库，对于"基础版小程序拉新 + 正式版 App 留存"的阶段性策略，这是最高效的路径。

---

## 二、系统架构

```
                         ┌───────────────────────┐
                         │      腾讯云 CloudBase  │
                         │                       │
                         │  ┌─────────────────┐  │
                         │  │  云函数 (SCF)    │  │
                         │  │  - sendSmsCode  │  │
                         │  │  - login        │  │
                         │  │  - importDeck   │  │
                         │  └────────┬────────┘  │
                         │           │           │
                         │  ┌────────▼────────┐  │
                         │  │   文档型数据库   │  │
                         │  │  - decks        │  │
                         │  │  - card_states  │  │
                         │  │  - trials       │  │
                         │  │  - upload_logs  │  │
                         │  └────────┬────────┘  │
                         │           │           │
                         │  ┌────────▼────────┐  │
                         │  │  云存储 (COS)    │  │
                         │  │  (图片/音频)     │  │
                         │  └────────┬────────┘  │
                         └───────────┼───────────┘
                                     │
            ┌────────────────────────┼────────────────────────┐
            │                        │                        │
            ▼                        ▼                        ▼
     微信小程序                   PWA / H5                独立 App
  (原生 SDK 直连)           (CloudBase SDK / REST)    (CloudBase REST)
  - db.collection()        - callFunction()          - HTTP API
  - wx.cloud.uploadFile()  - 云存储下载               - COS SDK
```

---

## 三、数据库设计：PostgreSQL → CloudBase 集合

### 3.1 现状（v4.x PostgreSQL 6 表）

| 表名 | 用途 | 主要字段 |
|------|------|---------|
| `cards_pool` | 卡片池 | card_id, card_name, deck_name, image_url, audio_url |
| `server_decks` | 牌组索引 | id, name, description, card_count |
| `server_deck_cards` | 牌组-卡片关联 | deck_id, card_id, sort_order |
| `upload_log` | 上传日志 | source_file, deck_name, card_count |
| `sync_trials` | 答题记录 | device_id, card_id, rating, response_time_ms, ... |
| `sync_card_states` | SRS 状态 | device_id, card_id, srs_stage, interval, due_date, ... |

### 3.2 目标（CloudBase 3 集合）

#### 集合 `decks` — 牌组与卡片（合并 3 表）

```json
{
  "_id": "deck_hash8",
  "name": "蔬菜",
  "description": "",
  "cardCount": 20,
  "sourceFile": "蔬菜.yhspack",
  "cards": [
    {
      "cardId": "hash_abc",
      "name": "苹果",
      "imageUrl": "storage/abc_img.jpg",
      "audioUrl": "storage/abc_aud.m4a",
      "sortOrder": 1
    },
    {
      "cardId": "hash_def",
      "name": "香蕉",
      "imageUrl": "storage/def_img.jpg",
      "audioUrl": "storage/def_aud.m4a",
      "sortOrder": 2
    }
  ],
  "createdAt": "2026-05-05T00:00:00Z",
  "updatedAt": "2026-05-05T00:00:00Z"
}
```

**为什么可以内嵌：** 牌组创建后很少修改卡片列表，且下载到本地后整个牌组一次性读取。不需要跨表 Join。最大一个牌组一般 < 200 张卡片，文档大小约 50-100KB，完全在云数据库单文档限制（1MB）内。

#### 集合 `card_states` — SRS 状态（原 sync_card_states）

```json
{
  "_id": "auto",
  "userId": "user_openid_or_uid",
  "stateKey": "deckHash8::cardHash_abc",
  "deckKey": "deckHash8",
  "cardId": "cardHash_abc",
  "srsStage": "review",
  "interval": 86400,
  "easeFactor": 1.3,
  "dueDate": "2026-05-06",
  "dueTs": 1746489600,
  "stepIndex": 0,
  "reviewMode": "T1",
  "lapsesStreak": 0,
  "lapsesTotal": 0,
  "suspended": false,
  "updatedAt": 1746403200
}
```

**索引：** `userId + stateKey` 联合唯一索引，`userId + deckKey` 索引

#### 集合 `trials` — 答题记录（原 sync_trials）

```json
{
  "_id": "auto",
  "userId": "user_openid_or_uid",
  "trialId": "uuid",
  "cardId": "cardHash_abc",
  "deckKey": "deckHash8",
  "sessionId": "session_uuid",
  "questionType": "T1",
  "rating": "good",
  "isCorrect": true,
  "attemptNumber": 1,
  "optionsShown": ["card_a", "card_b", "card_c"],
  "correctOption": "card_a",
  "distractorChosen": null,
  "responseTimeMs": 3500,
  "srsStageBefore": "review",
  "intervalBefore": 86400,
  "easeBefore": 1.3,
  "lapsesStreakBefore": 0,
  "lapsesTotalBefore": 0,
  "reviewModeBefore": "T1",
  "srsStageAfter": "review",
  "intervalAfter": 172800,
  "easeAfter": 1.3,
  "timestamp": 1746403200000,
  "createdAt": "2026-05-05T00:00:00Z"
}
```

**索引：** `userId + timestamp` 索引，`userId + deckKey` 索引

#### 集合 `upload_logs` — 上传日志（原 upload_log，仅 deck_manager 使用）

与原结构基本一致，增加 `userId` 字段。

---

## 四、迁移步骤

### 第一阶段：环境准备

1. 在腾讯云开通 **CloudBase 环境**
   - 套餐选个人版（19.9元/月）即可，后续按需升级
   - 选择国内节点（北京/上海/广州）
2. 开通 **短信服务**
   - 购买 1 万条套餐包（470元，有效期 2 年）
   - 申请短信签名和正文模板
3. 创建数据库集合
   - `decks`（读写权限：所有用户可读，仅管理员可写）
   - `card_states`（读写权限：仅创建者可读写）
   - `trials`（读写权限：仅创建者可读写）
   - `upload_logs`（读写权限：仅管理员可写）

### 第二阶段：数据迁移

4. 将 Supabase 中存量数据导出（PostgreSQL dump 或 CSV）
5. 编写迁移脚本：6 表 → 3 集合 ETL
   - `cards_pool` + `server_decks` + `server_deck_cards` → `decks`（按 deck_name 分组，cards 内嵌）
   - `sync_card_states` → `card_states`（逐条转换）
   - `sync_trials` → `trials`（逐条转换）
6. 将媒体文件从 Supabase Storage 迁移到 COS

### 第三阶段：云函数开发

7. 编写云函数 `sendSmsCode`
   - 接收手机号
   - 调用腾讯云 SMS 发送 6 位验证码
   - 验证码缓存到 Redis（或云数据库 5 分钟过期）
8. 编写云函数 `login`
   - 接收手机号 + 验证码（或微信 login code）
   - 校验验证码（或调用微信 code2session）
   - 签发自定义 JWT（含 userId）
9. 编写云函数 `importDeck`（可选，替代 deck_manager）

### 第四阶段：PWA 改造

10. 修改 `yihai_v4.7.html`（→ `yihai_v5.0.html`）
    - 删除 Supabase SDK（CDN script）
    - 登录 UI：邮箱+密码 → 手机号+验证码
    - `_sb.from(x).insert/upsert/select` → `cloud.callFunction()`
    - `_sb.storage.download()` → COS 签名 URL / 云存储下载 API

### 第五阶段：小程序开发（后续）

11. 微信开发者工具新建小程序项目
12. `app.js` 初始化 CloudBase
13. 页面复用 PWA 的 Vue/Vanilla 逻辑，渲染层改为 WXML
14. 数据库层直接用 `wx.cloud.database()` 直连

---

## 五、PWA 端改造明细（yh_v4.7.html → v5.0.html）

### 5.1 删除

| 位置 | 内容 | 理由 |
|------|------|------|
| CDN script | `<script src="supabase-js">` | 不再使用 Supabase |
| 常量 | `SUPABASE_URL`, `SUPABASE_ANON_KEY` | 替换为 CloudBase 配置 |
| 函数 | `restoreCloudSession()` | 替换为 JWT Token 恢复 |
| 函数 | `doCloudLogin()` | 替换为 `doCloudLogin(phone, code)` |
| 函数 | `doCloudLogout()` | 替换为清除本地 Token |

### 5.2 新增

| 位置 | 内容 |
|------|------|
| CDN script | `<script src="https://cdn.imgcache.qq.com/tcb/cloudbase-js-sdk/2.x/cloudbase.js">` |
| 常量 | `CLOUDBASE_ENV_ID`, `CLOUDBASE_REGION` |
| 工具函数 | `requestSmsCode(phone)` — 请求验证码 |
| 工具函数 | `cloudLogin(phone, code)` — 验证码登录 |
| 工具函数 | `cloudRequest(path, data)` — 统一 API 请求（携带 JWT） |

### 5.3 替换

| 原代码 | 替换为 |
|--------|--------|
| `_sb.auth.signInWithPassword(...)` | `cloudLogin(phone, code)` → 获取 JWT |
| `_sb.auth.getSession()` | 读取 localStorage 中的 JWT，检查过期 |
| `_sb.from('sync_trials').insert({...})` | `cloudRequest('/sync/trials', { body })` |
| `_sb.from('sync_card_states').upsert({...})` | `cloudRequest('/sync/states', { body })` |
| `_sb.from('server_decks').select('...')` | `cloudRequest('/decks')` |
| `_sb.from('server_deck_cards').select('...')` | `cloudRequest('/decks/' + deckId)` |
| `_sb.storage.from('ReminiSea').download(url)` | 云存储 `getTempFileURL(url)` 或 COS 签名 URL |

### 5.4 整体改动量

| 模块 | 改动类型 | 预估行数 |
|------|---------|---------|
| 配置与脚本 | 替换 | ~5 行 |
| Auth 登录/登出 | 重写 | ~50 行 |
| 数据同步（sync_trials/states） | 替换 API 调用 | ~60 行 |
| 云端牌组浏览（下载） | 替换 API 调用 | ~80 行 |
| 文件下载 | 替换存储层调用 | ~30 行 |
| UI 表单（登录界面） | 修改 | ~20 行 |
| **合计** | | **~250 行** |

---

## 六、小程序端设计（2026-05-10 修订）

### 6.1 技术方案：uni-app + 腾讯云开发

**结论：改用 uni-app。**

原方案选原生小程序是为了减少抽象层。经评估，以下因素改变了决策：

| 维度 | 原生小程序 | uni-app (Vue) | 结论 |
|---|---|---|---|
| 开发效率（AI 辅助） | 低 — WXML/WXSS 私有语法，多文件跳转 | 高 — HTML/CSS/JS 思维，与现有 PWA 一致 | uni-app 优 |
| 代码复用 | SRS 逻辑可复用，UI 全重写 | SRS 逻辑 + CSS 样式体系可直接搬 | uni-app 优 |
| 多端能力 | 仅小程序 | 一套代码 → 小程序 + H5 + 未来 App | uni-app 优 |
| 云开发调用 | `wx.cloud` 原生 | `wx.cloud` 直接可用，和原生一样 | 持平 |
| 性能 | 最优 | Vue 数据绑定轻微开销，非游戏场景无感知 | 持平 |
| 产物体积 | 最小 | 多 ~30KB 框架运行时 | 可接受 |

**H5 端是白送的** — 编译到 H5 后现有 PWA 用户 URL 不变，无缝过渡到新架构。

### 6.2 登录方案：微信登录替代 SMS 验证码

**结论：改用微信一键登录（`wx.login` → openid）。**

原方案选 SMS 验证码是为了和 App 统一用手机号。经评估，以下因素改变了决策：

| 维度 | SMS 验证码 | 微信登录 |
|---|---|---|
| 用户体验 | 输入手机号 + 等待 + 填验证码 | 点一下授权，免输入 |
| 个人主体支持 | ❌ 需企业资质申请签名 | ✅ 个人主体可用 |
| 费用 | 470 元/万条 | 免费 |
| 开发量 | 需云函数 + SMS 服务 | `wx.login` 3 行代码 |
| 跨端账号打通 | 手机号天然打通 | openid → 需额外绑定层 |

**选择微信登录的理由：**
1. 目标用户（老年人）最不能接受的就是"输入手机号等验证码"
2. 个人主体无法申请 SMS 签名，这是硬障碍
3. 小程序版本如果成功，后续 App 版本可以用微信开放平台 `unionid` 打通，或让用户在小程序内绑定手机号

### 6.3 个人主体发布可行性

| 限制项 | 是否影响 | 说明 |
|---|---|---|
| 微信支付 | ❌ 不影响 | 初期免费验证，不涉及付费 |
| web-view | ❌ 不影响 | 无内嵌网页场景 |
| 获取手机号 | ❌ 不影响 | `wx.login` 拿 openid 即可 |
| 云开发 | ❌ 不影响 | 个人主体完全支持 |
| 类目审核 | ⚠️ 需注意 | 类目选"教育-在线教育"，描述定位"记忆力训练/脑力锻炼"，避免医疗/诊断措辞 |

**审核方案：** 应用名"忆海拾光"，类目选**教育 > 在线教育**，功能描述"中老年脑力锻炼与记忆力训练工具"，不提及 AD/MCI/认知障碍/康复治疗。

### 6.4 页面结构

```
pages/
├── index/           # 主页：牌组列表 + 统计概览
├── practice/       # 练习页（浏览 + 测验，通过 tab 切换）
├── stats/          # 统计页（今日/历史/卡片列表）
├── settings/       # 设置页（多 tab：通用/SRS/云端）
├── cloud-decks/    # 云端牌组浏览 + 下载
└── card-detail/    # 卡片详情（SRS 状态、重置、解除暂停）
```

### 6.5 数据互通设计

- 小程序用 `openid` 标识用户（CloudBase 自动注入 `OPENID`）
- H5/App 通过微信开放平台网页授权获取同一 `unionid`
- CloudBase 数据库权限：`card_states`、`trials` 按 `userId` 隔离，仅创建者可读写
- 同一微信用户在小程序和 H5 上的数据天然打通（同 openid/unionid）

---

## 七、时间规划（2026-05-10 修订）

### PWA 现状评估

当前 `yihai_v4.9.html`：**5249 行，~247 KB**。~130 个函数。核心改造难度分布见下表。

### uni-app 迁移任务拆解

| 阶段 | 内容 | 预估 |
|---|---|---|
| **项目初始化** | uni-app 脚手架、pages.json、manifest.json、微信小程序配置、CloudBase SDK 集成 | 0.5 天 |
| **云开发环境** | CloudBase 环境开通、集合创建、安全规则配置 | 0.5 天 |
| **数据迁移** | Supabase ETL 脚本（6 表 → 3 集合）、存量导入、媒体文件迁移到 COS | 1-2 天 |
| **SRS + 存储适配** | processAnswer 等纯逻辑直接复用；IndexedDB → 文件系统；localStorage → wx.storage | 1 天 |
| **Supabase → CloudBase** | 所有数据库调用替换为 `wx.cloud.database()` 或 `wx.cloud.callFunction()` | 1-2 天 |
| **UI 组件重写** | 主页、练习页、完成页、统计页、设置页、云端牌组页、卡片详情页（Vue 单文件组件） | 4-5 天 |
| **TTS + 音频适配** | speechSynthesis → 微信 TTS 插件；Audio → InnerAudioContext；AudioContext tone 改预录制文件 | 1-2 天 |
| **Canvas + 导入** | confetti 动效迁移；.yhspack 导入流程适配（wx.chooseMessageFile + JSZip） | 0.5 天 |
| **H5 端适配** | uni-app 条件编译下 H5 端测试和兼容处理 | 0.5 天 |
| **测试 + 调修** | 真机测试、审核反馈修复 | 2-3 天 |

**总计：12-17 天**（不含 SMS，改用微信登录）

### 独立 App 额外工作量（仅做参考，非当期）

| 内容 | 预估 |
|---|---|
| uni-app 编译到 iOS + Android | 已有代码直接编译 |
| 苹果开发者账号（$99/年） + 国内 App 备案 | 1-2 周等待期 |
| 医疗器械资质确认（如苹果认定医疗类） | 不确定 |
| App Store 审核调修 | 2-5 天 |

**关键风险：** 国内 App Store 需服务器 ICP 备案 + App 通信管理局备案，缺一不可。医疗健康类可能要求《医疗器械注册证》。建议小程序验证产品后再评估独立 App。

---

## 八、风险与决策（2026-05-10 更新）

### 已知风险
1. ~~**腾讯云 SMS 签名审核**~~ — 已排除。改用微信登录 `wx.login`，个人主体可用，免费。
2. **微信小程序审核** — 类目选"教育-在线教育"，定位"记忆力训练"，避开医疗措辞。个人主体通常可过。
3. **NoSQL 数据一致性** — 无事务保证，需要应用层处理并发
4. **Supabase 到 CloudBase 的同步过渡期** — 迁移期间可先单写 CloudBase，Supabase 留作冷备
5. **个人主体限制** — 无微信支付、无 web-view、无获取手机号。当前版本不受影响，但未来如需付费功能需升级为企业主体（300 元/年认证费）
6. **国内 App Store 合规（如做独立 App）** — 需 ICP 备案 + App 备案 + 隐私政策 + 用户单独同意敏感信息。医疗健康类可能要求《医疗器械注册证》

### 已决事项
- [x] **uni-app** — 一套代码出小程序 + H5，Vue 语法与现有 PWA HTML/CSS 思维一致
- [x] **微信登录** — `wx.login` 获取 openid，免密一键登录，个人主体可用
- [x] **个人主体发布** — 类目选"教育-在线教育"，避开医疗措辞，初期免费验证
- [x] **牌组管理内化** — 废弃 deck_manager_v1，功能并入训练 App
- [x] **H5 保留** — uni-app 编译到 H5，现有 PWA 用户无缝过渡

### 待决策项
- [ ] 是否保留 Supabase 数据作为只读冷备？
- [ ] 迁移窗口期：停服迁移还是双写过渡？
- [ ] 后续独立 App 是否需要医疗器械资质？（取决于苹果审核对"健康"vs"医疗"的界定）
- [ ] 是否需要兼容 v4.x 离线导出的 `.yhspack` 文件导入到 CloudBase？
