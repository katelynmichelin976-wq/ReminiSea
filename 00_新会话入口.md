# 忆海拾光 · 新会话上下文恢复入口

> 新对话开始时，告诉 Claude：
> **「继续忆海拾光项目，读取 00_新会话入口.md」**

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| 训练 App 当前版本 | **v4.9**（已提交，2026-05-06） |
| 牌组管理工具 | **deck_manager_v1.html**（Supabase 集成） |
| 制卡工具 | **index_v49.html**（暂停迭代） |
| 源码仓库 | https://github.com/katelynmichelin976-wq/ReminiSea |
| 线上地址 | https://katelynmichelin976-wq.github.io/gemi/ |
| Supabase URL | https://juzkonrzfyvchqxzmlpr.supabase.co |
| 卡片数据格式 | `.yhspack`（ZIP，内含 deck.json + media/） |
| 正式用户 | **chenlian@263.net**（妈妈使用） |
| 测试用户 | **zyhacl@gmail.com**（开发迭代 + Claude 测试） |

---

## 二、当前状态（v4.9 已提交，2026-05-06）

### v4.9 新增（本轮提交）

- ✅ **syncAll 先 pull 后 push** — 配置同步顺序调换，避免多设备场景本地空配置覆盖远端修改
- ✅ **getDeckStatsSrs 新卡统计修复** — 无 CardState 的卡片（刚下载/导入）计入新卡数，修复下载后显示 0/0
- ✅ **MEDIA_CDN_BASE 停用** — 暂不启用腾讯云 COS 回源，媒体下载走 Supabase Storage，避免下载费用
- ✅ **syncCardState 补 user_id** — upsert 增加 user_id 字段（上次遗漏），修复卡片状态静默上传失败
- ✅ **Playwright 网络版回归测试** — `_playwright_cloud_test.js`（17 断言：登录→下载云端牌组→练习同步→配置同步→多设备同步→退出）
- ✅ **多设备同步测试** — 双 browser context 模拟两台设备，验证深色模式配置跨设备同步

### v4.8 新增（历史提交）

- ✅ **代码审查修复（4项）** — XSS `esc()` 补全、`deleteDeck` 改用 `showConfirmDialog`、ObjectURL 泄漏 `setObjURL()` 统一管理、`importYhspack` 图片 MIME 按扩展名推导
- ✅ **SRS 测试日期偏移修复** — `todayStr`/`addDays` 改为 UTC 时间，SRS 67 测试全通过
- ✅ **syncPendingData 并发批次** — TrialLog + CardState 补传改为 `parallelMapLimit 10` 路并发
- ✅ **cloudPushConfig key 动态提取** — NUM_KEYS/BOOL_KEYS/STR_KEYS 改为从 `SRS_CONFIG` 动态推导类型，消除双份维护
- ✅ **v4.8 单元测试** — `yihai_v4.8_test.js`（46 tests：minsToTs/cdnMediaUrl/secsToLabel/parallelMapLimit/setObjURL）
- ✅ **多用户数据隔离** — `sync_trials` + `sync_card_states` 加 `user_id` 列，RLS 改为 `auth.uid() = user_id`，唯一约束改为 `(user_id, state_key)`，前端缓存 `_cloudUserId`
- ✅ **用户账号拆分** — chenlian@263.net（正式）vs zyhacl@gmail.com（测试），已清理正式账号中的测试数据

### 遗留 / 下一步

1. **角色系统** — 患者（大按钮简化练习）vs 照护者（全功能管理）
2. **跨牌组自动练习** — 患者模式按比例混合多牌组

---

## 三、工作文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 训练 App | `C:\code\yihai_v4.9.html` | v4.9，含多用户隔离、并发下载、参数云同步、配置先拉后推 |
| 牌组管理工具 | `C:\code\deck_manager_v1.html` | Supabase 集成，四步流程 |
| 制卡工具 | `C:\code\index_v49.html` | 暂停迭代 |
| 历史版本 | `C:\code\archive/` | v4.3–v4.8 |
| SRS 设计规格 | `C:\code\docs\srs_design_v6.9.md` | 权威依据 |
| SRS 单元测试 | `C:\code\tests\srs_test.js` | 67 测试全通过 |
| v4.4 函数测试 | `C:\code\tests\yihai_v4.4_test.js` | 98 测试 |
| v4.8 函数测试 | `C:\code\tests\yihai_v4.8_test.js` | 46 测试 |
| Playwright 单机测试 | `C:\code\tests\_playwright_test.js` | 18 断言 |
| Playwright 网络版测试 | `C:\code\tests\_playwright_cloud_test.js` | 17 断言 |
| Supabase Schema | `C:\code\sql\supabase_schema.sql` | 建表 SQL |
| Storage 策略 | `C:\code\sql\supabase_storage_policies.sql` | Bucket RLS |
| CLAUDE.md | `C:\code\CLAUDE.md` | 项目指引 |
| 开发问答 | `C:\code\docs\yihai_开发问答.md` | 开发过程中用户疑问记录 |
| 实现说明 | `C:\code\docs\yihai_实现说明.md` | 跨模块实现流程、同步、统计、语音说明 |

---

## 四、启动开发环境

```bash
# 启动本地 HTTP 服务器
python -m http.server 8080 --directory /c/code
```

- 训练端：http://localhost:8080/yihai_v4.9.html
- 管理工具：http://localhost:8080/deck_manager_v1.html
- 制卡工具：http://localhost:8080/index_v49.html

---

## 五、Supabase 凭据

```
URL:       https://juzkonrzfyvchqxzmlpr.supabase.co
Anon Key:  sb_publishable_gKEnRcaiEI9eP00jJivbOA_xQ2Z1cCD
Bucket:    ReminiSea
```

### 表结构

| 表 | 用途 |
|------|------|
| cards_pool | 卡池（所有上传的原始卡片） |
| server_decks | 服务端题库 |
| server_deck_cards | 题库-卡片关联（含排序） |
| upload_log | 上传操作审计 |
| sync_trials | 训练端答题记录（v4.4 已接入） |
| sync_card_states | 训练端 SRS 状态（v4.4 已接入） |
| sync_config | 参数配置云端同步（v4.8 新增） |

---

## 六、关键架构决策

1. **单文件原则** — 所有工具都是独立 HTML 文件
2. **simpleHash 一致性** — 制卡工具、管理工具、训练端共用相同实现
3. **卡片不做自动去重** — 用户手动整理
4. **合并策略默认「保留两者」** — 最安全
5. **训练端同步静默非阻塞** — 所有 Supabase 调用 try/catch，失败不影响训练
6. **增量同步** — 基于 cards_pool.updated_at + 本地 _imgUrl/_audUrl 对比，只拉变更
7. **会话持久化** — Supabase SDK 自动管理 session，重启恢复
8. **同步进度条** — `showSyncProg` / `hideSyncProg` 操作 `cloud-progress` 元素，每步骤更新文本和分数

---

## 七、函数速查

### v4.9 变更

| 函数 | 说明 |
|------|------|
| `syncAll()` 顺序调整 | 先 `cloudPullConfig()` 再 `cloudPushConfig()` |
| `getDeckStatsSrs()` | 无 CardState 卡片计入新卡数 |
| `MEDIA_CDN_BASE = ''` | 停用 COS 回源 |

### v4.8 新增

| 函数 | 说明 |
|------|------|
| `cloudPushConfig()` | 收集 SRS + UI 参数 push 到 sync_config（key 从 SRS_CONFIG 动态推导） |
| `cloudPullConfig()` | 从 sync_config 拉取并应用到本地 |
| `debouncePushConfig()` | 设置变更防抖触发 push |
| `parallelMapLimit(arr, limit, fn)` | 并发限制辅助函数，最多 limit 个异步并行 |
| `showSyncProg(curr, total, text)` | 同步进度条：当前步骤/总步数 + 描述 |
| `hideSyncProg()` | 隐藏同步进度条 |
| `setObjURL(card, field, blob)` | 创建 ObjectURL，自动 revoke 旧值防泄漏 |
| `cdnMediaUrl(path)` | 构造 CDN 媒体 URL |
| `secsToLabel(secs)` | 秒→标签（如 3600→"1h"） |
| `_cloudUserId` | 当前登录用户的 UUID 缓存 |

### v4.7 变更

| 函数 | 说明 |
|------|------|
| `syncAll(deckKey, showToast?, noDecks?)` | 双向同步，showToast 控制 toast 显示，noDecks 限制为仅状态+配置（轻量） |
| `_sessionFirstRatings` | 变量，按卡·首次评级统计对象（state_key → rating） |
| 统计页 KPI | 良好/困难/重来（按卡首次评级），取代旧 答对/答错/正确率 |

| 函数 | 说明 |
|------|------|
| `audioMimeFromUrl(url)` | 根据扩展名推导 audio MIME（iOS 兼容） |
| `backfillAfterPractice(deckKey)` | 练习完成后触发 syncAll |
| `_realtimeUpload` | 开关变量，控制学习中是否实时上传 |

### v4.4 历史函数

| 函数 | 说明 |
|------|------|
| `restoreCloudSession()` | 启动时恢复 Supabase 会话 |
| `doCloudLogin()` / `doCloudLogout()` | 登录/退出 |
| `updateCloudTabUI()` | 云端 Tab 状态切换 |
| `refreshServerDeckList()` | 获取 server_decks 列表 |
| `downloadDeckFromCloud(deckId, deckName)` | 全量下载牌组 |
| `syncDeckFromCloud(deckId, deckName)` | 增量同步牌组 |
| `syncTrialLog(entry)` | 上传答题记录 |
| `syncCardState(state)` | 上传 SRS 状态 |
| `showCloudToast(msg)` | 云端操作提示 |

---

## 八、已解决的坑

| 问题 | 根因 | 修复 |
|------|------|------|
| Supabase 登录 400 | 邮箱未确认 | Dashboard 关闭 Confirm email |
| Storage 上传 403 | RLS 策略缺失 | 对 storage.objects 加四条策略 |
| SDK upload 失败 | contentType 参数不兼容 | 去掉，让 SDK 自动推断 |
| Storage 中文路径 | deck.name 含中文 | 改用 deck.id（纯 HEX） |
| 导出 0 张卡 | .filter(cc => cc.image) 过滤 | 去掉过滤条件 |
| 云端列表显示原名 | 查 cards_pool.deck_name | 改为查 server_decks.name |
| 下载后 currentDeck 未切换 | downloadDeckFromCloud 缺 currentDeck=key | 在 renderDeckList 前设置 |
